jdodson.org uses:
* jekyll 3
* bootstrap 3.0.1 w the Flatly http://bootswatch.com/ theme

gem install jekyll

# serve the pages and regenerate when there is a change
jekyll serve --watch

# build a fresh version of the site
jekyll build
